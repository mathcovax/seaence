services:
  spotter:
    image: 'ghcr.io/mathcovax/seaence_spotter:${TAG}'
    command: npm -w services/spotter run dev
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    ports:
      - published: 7000
        target: 80